% ============================================================
%  PulseBoard – Full Technical Documentation
%  Compile with: pdflatex -> pdflatex x2
%  Requires packages listed in the preamble.
% ============================================================

\documentclass[12pt,a4paper]{report}

% ── Fonts & Encoding ────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}

% ── Page Layout ─────────────────────────────────────────────
\usepackage[
  top=2.5cm, bottom=2.5cm,
  left=2.8cm, right=2.8cm,
  headheight=15pt
]{geometry}

% ── Colour ──────────────────────────────────────────────────
\usepackage[dvipsnames,svgnames,table]{xcolor}
\definecolor{accent}{HTML}{CCF900}
\definecolor{darkbg}{HTML}{050505}
\definecolor{codebg}{HTML}{1E1E2E}
\definecolor{codefg}{HTML}{CDD6F4}
\definecolor{keyword}{HTML}{89DCEB}
\definecolor{string}{HTML}{A6E3A1}
\definecolor{comment}{HTML}{6C7086}
\definecolor{number}{HTML}{FAB387}
\definecolor{type}{HTML}{89B4FA}
\definecolor{func}{HTML}{CBA6F7}
\definecolor{tablebg}{HTML}{F8FAFC}
\definecolor{tableheadbg}{HTML}{1E293B}
\definecolor{rowodd}{HTML}{F1F5F9}
\definecolor{roweven}{HTML}{FFFFFF}
\definecolor{flowblue}{HTML}{3B82F6}
\definecolor{flowgreen}{HTML}{10B981}
\definecolor{floworange}{HTML}{F59E0B}
\definecolor{flowred}{HTML}{EF4444}
\definecolor{flowpurple}{HTML}{8B5CF6}

% ── Graphics & Floats ───────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage{wrapfig}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,fit,
                 backgrounds,shadows,matrix,calc,trees,chains}

% ── TikZ Common Styles ──────────────────────────────────────
\tikzset{
  proc/.style={rectangle, rounded corners=4pt, draw=#1!70, fill=#1!10,
               text width=4.8cm, align=center, font=\small,
               minimum height=0.85cm, inner sep=4pt},
  procS/.style={rectangle, rounded corners=4pt, draw=#1!70, fill=#1!10,
               text width=3.2cm, align=center, font=\small,
               minimum height=0.8cm, inner sep=3pt},
  dec/.style={diamond, draw=floworange!80, fill=floworange!12,
              align=center, font=\small, aspect=2.2, inner sep=2pt},
  term/.style={rectangle, rounded corners=18pt, draw=accent!80!black,
               fill=accent!25, align=center, font=\small\bfseries,
               minimum width=2.8cm, minimum height=0.7cm},
  arr/.style={-{Stealth[scale=1.1]}, thick, #1},
  ehead/.style={rectangle, draw=tableheadbg, fill=tableheadbg, text=white,
                minimum width=7.5cm, align=center, font=\small\bfseries,
                inner sep=5pt, minimum height=0.75cm},
  eodd/.style={rectangle, draw=type!25, fill=rowodd,
               minimum width=7.5cm, align=left, font=\small\ttfamily,
               inner sep=4pt, minimum height=0.6cm},
  eeven/.style={rectangle, draw=type!25, fill=roweven,
                minimum width=7.5cm, align=left, font=\small\ttfamily,
                inner sep=4pt, minimum height=0.6cm},
  actor/.style={rectangle, rounded corners=3pt, draw=tableheadbg,
                fill=tableheadbg, text=white, font=\small\bfseries,
                minimum width=2.4cm, minimum height=0.65cm, align=center},
  smsg/.style={-{Stealth}, thick, darkbg},
  sret/.style={-{Stealth}, thick, dashed, type},
  sll/.style={dashed, gray!40, line width=0.4pt},
  tnode/.style={rectangle, rounded corners=3pt, draw=type!60, fill=type!10,
                font=\footnotesize, align=center, minimum height=0.55cm,
                minimum width=2.2cm, inner sep=3pt},
  troot/.style={rectangle, rounded corners=4pt, draw=accent!80!black,
                fill=accent!25, font=\small\bfseries, align=center,
                minimum height=0.65cm, minimum width=3cm},
}

% ── Tables ──────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{array}
\usepackage{colortbl}

% ── Hyperlinks ──────────────────────────────────────────────
\usepackage[
  colorlinks = true,
  linkcolor  = accent!70!black,
  urlcolor   = type,
  citecolor  = accent!70!black,
  pdftitle   = {PulseBoard Technical Documentation},
  pdfauthor  = {PulseBoard Team}
]{hyperref}

% ── Headers & Footers ───────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{gray}{\textbf{PulseBoard} — Technical Documentation}}
\fancyhead[R]{\small\textcolor{gray}{\leftmark}}
\fancyfoot[C]{\small\textcolor{gray}{\thepage}}
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0pt}

% ── Typography & Misc ───────────────────────────────────────
\usepackage{microtype}
\usepackage{parskip}
\usepackage{enumitem}
\usepackage{mdframed}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{fontawesome5}
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{setspace}
\usepackage{soul}

% ── Custom Environments ─────────────────────────────────────
\newenvironment{infobox}[1]{%
  \begin{tcolorbox}[
    enhanced, breakable,
    colback  = type!8,
    colframe = type!60,
    fonttitle= \bfseries\small,
    title    = {#1},
    left=6pt, right=6pt, top=4pt, bottom=4pt
  ]
}{%
  \end{tcolorbox}
}

\newenvironment{warnbox}[1]{%
  \begin{tcolorbox}[
    enhanced, breakable,
    colback  = number!8,
    colframe = number!60,
    fonttitle= \bfseries\small,
    title    = {#1},
    left=6pt, right=6pt, top=4pt, bottom=4pt
  ]
}{%
  \end{tcolorbox}
}

% ── Custom Commands ─────────────────────────────────────────
\newcommand{\code}[1]{\texttt{\small\color{type}#1}}
\newcommand{\endpoint}[2]{\texttt{\textbf{\color{keyword}#1}\color{codefg!80!white}\ #2}}
\newcommand{\method}[1]{\colorbox{type!20}{\texttt{\small\bfseries\color{type}#1}}}
\newcommand{\badge}[2]{\colorbox{#1!20}{\texttt{\small\color{#1!80!black}#2}}}

% ── Section Styling ─────────────────────────────────────────
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{darkbg}}
  {\textcolor{accent}{\chaptertitlename\ \thechapter}}
  {20pt}
  {\Huge}
  [\vspace{-5pt}\textcolor{accent}{\rule{\textwidth}{2pt}}]

\titleformat{\section}
  {\normalfont\Large\bfseries\color{darkbg}}
  {\textcolor{accent}{\thesection}}
  {1em}{}

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{darkbg!80}}
  {\textcolor{type}{\thesubsection}}
  {1em}{}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\color{darkbg!60}}
  {\textcolor{func}{\thesubsubsection}}
  {1em}{}

% ─────────────────────────────────────────────────────────────
%  DOCUMENT
% ─────────────────────────────────────────────────────────────
\begin{document}

% ── Cover Page ──────────────────────────────────────────────
\begin{titlepage}
\pagecolor{darkbg}
\color{white}
\centering
\vspace*{2cm}

\begin{tikzpicture}
  \node[rectangle, fill=accent, minimum width=14cm, minimum height=1.4cm,
        rounded corners=4pt] (banner) {};
  \node[font=\Huge\bfseries\itshape, text=darkbg] at (banner)
    {PULSEBOARD};
\end{tikzpicture}

\vspace{0.6cm}
{\Large\bfseries\color{white!70} Technical Documentation}

\vspace{0.3cm}
{\large\color{white!50}\texttt{v1.0.0 — Production Release}}

\vspace{2cm}
\begin{tikzpicture}
  \draw[color=accent!30, line width=0.5pt] (0,0) -- (10,0);
\end{tikzpicture}

\vspace{1.5cm}
\begin{tabular}{rl}
  \textcolor{accent}{\faServer}   & \textcolor{white!80}{Node.js + Express + TypeScript} \\[6pt]
  \textcolor{accent}{\faDatabase} & \textcolor{white!80}{MongoDB + Mongoose ODM}          \\[6pt]
  \textcolor{accent}{\faMobile}   & \textcolor{white!80}{React Native + Expo Router}      \\[6pt]
  \textcolor{accent}{\faRobot}    & \textcolor{white!80}{Groq AI (Llama 3.1 8B)}          \\[6pt]
  \textcolor{accent}{\faEnvelope} & \textcolor{white!80}{Gmail API + OAuth 2.0}            \\
\end{tabular}

\vspace{2cm}
\begin{tikzpicture}
  \draw[color=accent!30, line width=0.5pt] (0,0) -- (10,0);
\end{tikzpicture}

\vspace{1cm}
{\color{white!50} Prepared by} \\[4pt]
{\large\bfseries\color{white} PulseBoard Development Team} \\[4pt]
{\color{accent}\texttt{IIT Jodhpur — 2026}}

\vfill
{\small\color{white!30}\texttt{SYS.READY · SECURE CONNECTION ESTABLISHED}}
\end{titlepage}
\pagecolor{white}
\color{black}

% ── Table of Contents ───────────────────────────────────────
\tableofcontents
\newpage

% ─────────────────────────────────────────────────────────────
\chapter{Project Overview}
% ─────────────────────────────────────────────────────────────

\section{What is PulseBoard?}

PulseBoard is a \textbf{smart campus intelligence platform} built for IIT Jodhpur students. It aggregates campus events from multiple sources — Gmail inboxes, club email channels, and direct club postings — and presents them in a unified, beautiful mobile interface.

The system uses \textbf{AI-powered email parsing} to automatically convert unstructured email content into structured event cards, eliminating the need for manual event submission. Students connect their Google account once and the platform continuously monitors their inbox, extracting campus-relevant information while aggressively filtering spam.

\begin{infobox}{Core Capabilities}
\begin{itemize}[leftmargin=*]
  \item \textbf{Smart Inbox} — Gmail-connected AI email parser using Groq LLM
  \item \textbf{Club Feed} — Real-time event feed from followed clubs
  \item \textbf{LHC Heatmap} — Lecture Hall Complex room availability grid
  \item \textbf{Smart Calendar} — Time-aware event calendar with category filtering
  \item \textbf{Push Reminders} — 1-hour pre-event push notifications
  \item \textbf{SOS Protocol} — Emergency contacts with one-tap calling
  \item \textbf{Club Admin Portal} — Clubs publish events directly from the app
\end{itemize}
\end{infobox}

\section{Technology Stack}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\bfseries}l X l}
\rowcolor{tableheadbg}
\color{white}Layer & \color{white}Technology & \color{white}Version \\
\toprule
Backend Runtime  & Node.js + TypeScript                    & 20 LTS / 5.9 \\
Web Framework    & Express.js                              & 4.21 \\
Database         & MongoDB + Mongoose ODM                  & Atlas / 9.1 \\
Mobile Framework & React Native (Expo Managed)             & 0.81.5 / 54 \\
Routing          & Expo Router (file-based)                & 6.0 \\
AI Parsing       & Groq API — Llama 3.1 8B Instant         & Latest \\
Email Access     & Gmail REST API + OAuth 2.0              & v1 \\
File Storage     & Cloudinary CDN                          & 1.41 \\
Email Delivery   & Nodemailer (SMTP) + SendGrid            & 8.0 / 8.1 \\
Push Notifications & Expo Push Notification Service        & 0.32 \\
Styling          & NativeWind (Tailwind CSS for RN)        & 4.2 \\
Animations       & Moti + React Native Reanimated          & 0.30 / 4.1 \\
\bottomrule
\end{tabularx}
\caption{Technology Stack Summary}
\end{table}

\section{Repository Structure}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  folder/.style={rectangle, rounded corners=3pt, draw=flowblue!60, fill=flowblue!10,
                 font=\small\bfseries, align=center, minimum height=0.6cm,
                 minimum width=3.0cm, inner sep=3pt},
  file/.style={rectangle, draw=gray!40, fill=gray!6,
               font=\footnotesize\ttfamily, align=left, minimum height=0.5cm,
               minimum width=3.0cm, inner sep=3pt},
  root/.style={rectangle, rounded corners=4pt, draw=accent!80!black, fill=accent!20,
               font=\small\bfseries, align=center, minimum height=0.7cm,
               minimum width=3.5cm},
  arr/.style={-{Stealth[scale=0.8]}, gray!60},
]

% Root
\node[root] (root) at (0,0) {PulseBoard/};

% Backend column
\node[folder, below left=0.8cm and 2.8cm of root] (be) {pulseboard\_backend/};
\node[file,   below=0.15cm of be] (be1) {src/server.ts};
\node[file,   below=0.05cm of be1] (be2) {src/controllers/  (11)};
\node[file,   below=0.05cm of be2] (be3) {src/routes/  (11)};
\node[file,   below=0.05cm of be3] (be4) {src/models/  (7)};
\node[file,   below=0.05cm of be4] (be5) {src/services/  (9)};
\node[file,   below=0.05cm of be5] (be6) {src/jobs/  cron};
\node[file,   below=0.05cm of be6] (be7) {src/middlewares/};
\node[file,   below=0.05cm of be7] (be8) {package.json};

% Mobile column
\node[folder, below right=0.8cm and 2.8cm of root] (mo) {pulseboard\_mobile/};
\node[file,   below=0.15cm of mo] (mo1) {app/\_layout.tsx};
\node[file,   below=0.05cm of mo1] (mo2) {app/index.tsx};
\node[file,   below=0.05cm of mo2] (mo3) {app/auth/  (login, register\ldots)};
\node[file,   below=0.05cm of mo3] (mo4) {app/tabs/  (5 screens)};
\node[file,   below=0.05cm of mo4] (mo5) {app/club\_tabs/  (admin)};
\node[file,   below=0.05cm of mo5] (mo6) {src/api/  client + helpers};
\node[file,   below=0.05cm of mo6] (mo7) {src/context/  ThemeContext};
\node[file,   below=0.05cm of mo7] (mo8) {package.json};

\draw[arr] (root.south) -- ++(0,-0.25) -| (be.north);
\draw[arr] (root.south) -- ++(0,-0.25) -| (mo.north);

\end{tikzpicture}
\caption{Repository Directory Structure}
\end{figure}

% ─────────────────────────────────────────────────────────────
\chapter{Backend Architecture}
% ─────────────────────────────────────────────────────────────

\section{Server Initialization}

The backend is a \textbf{TypeScript Express server} that connects to MongoDB on startup and immediately launches three independent background services.

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                             (start) {npm run dev};
  \node[proc=flowblue, below=of start]    (mw)    {Register Middleware\\(cors, morgan, json)};
  \node[proc=flowblue, below=of mw]       (routes){Mount API Routes\\(10 route groups under /api)};
  \node[proc=flowgreen, below=of routes]  (mongo) {mongoose.connect()\\MongoDB Atlas};
  \node[proc=flowpurple, below=of mongo]  (cron)  {initCronJobs()\\Daily cleanup at 00:00};
  \node[proc=floworange, below=of cron]   (gw)    {startGmailWatcher()\\Poll every 5 minutes};
  \node[proc=floworange, below=of gw]     (rs)    {startReminderScheduler()\\Check every 5 minutes};
  \node[term, below=of rs]                (listen){app.listen(8082)\\Server ready};

  \draw[arr=darkbg] (start)  -- (mw);
  \draw[arr=darkbg] (mw)     -- (routes);
  \draw[arr=darkbg] (routes) -- (mongo);
  \draw[arr=darkbg] (mongo)  -- (cron);
  \draw[arr=darkbg] (cron)   -- (gw);
  \draw[arr=darkbg] (gw)     -- (rs);
  \draw[arr=darkbg] (rs)     -- (listen);
\end{tikzpicture}
\caption{server.ts — Startup Sequence}
\end{figure}

\section{API Endpoints Reference}

\subsection{Authentication Routes \texttt{/api/auth}}

\begin{longtable}{>{\ttfamily}l >{\ttfamily}l p{6cm} c}
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endfirsthead
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endhead
\toprule
\rowcolor{rowodd}  POST & /register            & Register with email \& password. Sends OTP.   & \faLock[regular] \\
\rowcolor{roweven} POST & /verify-otp          & Verify email with 6-digit OTP.                & \faLock[regular] \\
\rowcolor{rowodd}  POST & /resend-otp          & Resend verification OTP.                      & \faLock[regular] \\
\rowcolor{roweven} POST & /login               & Email/password login. Returns JWT.            & \faLock[regular] \\
\rowcolor{rowodd}  POST & /google/callback     & Google OAuth. Accepts id\_token or code.      & \faLock[regular] \\
\rowcolor{roweven} POST & /forgot-password     & Request password reset OTP.                   & \faLock[regular] \\
\rowcolor{rowodd}  POST & /verify-reset-otp    & Validate the reset OTP.                       & \faLock[regular] \\
\rowcolor{roweven} POST & /reset-password      & Set new password after OTP verified.          & \faLock[regular] \\
\bottomrule
\end{longtable}

\subsection{Club Routes \texttt{/api/clubs}}

\begin{longtable}{>{\ttfamily}l >{\ttfamily}l p{6cm} c}
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endfirsthead
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endhead
\toprule
\rowcolor{rowodd}  POST  & /                  & Create new club.                              & \faKey \\
\rowcolor{roweven} POST  & /:clubId/follow    & Toggle follow / unfollow.                     & \faKey \\
\rowcolor{rowodd}  GET   & /                  & List all clubs.                               & \faLock[regular] \\
\rowcolor{roweven} GET   & /:id               & Get single club by ID.                        & \faLock[regular] \\
\rowcolor{rowodd}  PATCH & /:clubId/email     & Update club's inbound email address.          & \faLock[regular] \\
\bottomrule
\end{longtable}

\subsection{Event Routes \texttt{/api/events}}

\begin{longtable}{>{\ttfamily}l >{\ttfamily}l p{6cm} c}
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endfirsthead
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endhead
\toprule
\rowcolor{rowodd}  POST & /                  & Create event (multipart/form-data + image).   & \faLock[regular] \\
\rowcolor{roweven} GET  & /feed              & Global event feed (LIVE + UPCOMING).          & \faLock[regular] \\
\rowcolor{rowodd}  GET  & /club/:clubId      & Events for specific club.                     & \faLock[regular] \\
\bottomrule
\end{longtable}

\subsection{Personal Events \texttt{/api/personal-events}}

\begin{longtable}{>{\ttfamily}l >{\ttfamily}l p{6cm} c}
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endfirsthead
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Endpoint & \color{white}\normalfont Description & \color{white}\normalfont Auth \\
\endhead
\toprule
\rowcolor{rowodd}   GET    & /              & Fetch user's AI-parsed Gmail events (respects muted categories). Returns \texttt{\{events, mutedCategories\}}. & \faKey \\
\rowcolor{roweven}  POST   & /              & Create a manual event. Body: \texttt{title, date, timeDisplay, location, description?, category?, icon?, color?}. & \faKey \\
\rowcolor{rowodd}   DELETE & /:id           & Delete a personal event by ID (ownership-checked). & \faKey \\
\rowcolor{roweven}  PATCH  & /preferences   & Update muted categories. Body: \texttt{\{mutedCategories: string[]\}}. & \faKey \\
\rowcolor{rowodd}   POST   & /rescan        & Delete cache and force fresh Gmail scan.      & \faKey \\
\bottomrule
\end{longtable}

\begin{infobox}{User Control Design}
The \textbf{Smart Inbox} gives users full control over their event feed:
\begin{itemize}[leftmargin=*]
  \item \textbf{Delete} — any event (AI-parsed or manual) can be removed; \texttt{findOneAndDelete(\{\_id, userId\})} ensures users can only delete their own events.
  \item \textbf{Create} — manual events are stored with \texttt{gmailMessageId: "manual\_<userId>\_<timestamp>"} to avoid collisions with Gmail-parsed entries.
  \item \textbf{Mute} — per-category mute preferences are stored on the \texttt{User} document as \texttt{mutedCategories: string[]}. The GET endpoint excludes muted categories via a MongoDB \texttt{\$nin} query, so muted events are never sent to the client.
\end{itemize}
\end{infobox}

\subsection{Other Routes}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\ttfamily}l >{\ttfamily}l X}
\rowcolor{tableheadbg}
\color{white}\normalfont Method & \color{white}\normalfont Route & \color{white}\normalfont Description \\
\toprule
POST  & /api/email/inbound          & Mailgun webhook — receives club emails          \\
GET   & /api/calendar/events        & Calendar events (filterable by category \& date) \\
GET   & /api/lhc/heatmap            & LHC room bookings for a given date              \\
GET   & /api/users/me               & Authenticated user profile                      \\
PATCH & /api/users/me               & Update display name. Body: \texttt{\{name\}}    \\
POST  & /api/users/save-push-token  & Register Expo push token                        \\
GET   & /api/categories             & All event categories                            \\
\bottomrule
\end{tabularx}
\caption{Remaining API Endpoints}
\end{table}

% ─────────────────────────────────────────────────────────────
\chapter{Data Models}
% ─────────────────────────────────────────────────────────────

\section{User Model}

\begin{figure}[H]
\centering
\begin{tikzpicture}
  \node[ehead]                        (h)  {User};
  \node[eodd,  below=0pt of h]        (f1) {name:String · required};
  \node[eeven, below=0pt of f1]       (f2) {email:String · unique · indexed};
  \node[eodd,  below=0pt of f2]       (f3) {provider:'local' | 'google'};
  \node[eeven, below=0pt of f3]       (f4) {googleId?:String · unique};
  \node[eodd,  below=0pt of f4]       (f5) {password?:String · bcrypt hashed};
  \node[eeven, below=0pt of f5]       (f6) {isVerified:Boolean · default: false};
  \node[eodd,  below=0pt of f6]       (f7) {otp? / otpExpiry?:6-digit OTP · 10-min TTL};
  \node[eeven, below=0pt of f7]       (f8) {resetOtp? / resetOtpExpires?:15-min TTL};
  \node[eodd,  below=0pt of f8]       (f9) {expoPushToken?:Expo push token};
  \node[eeven, below=0pt of f9]       (fa) {year? / branch?:Academic info};
  \node[eodd,  below=0pt of fa]       (fb) {following:Number[] · clubIds followed};
  \node[eeven, below=0pt of fb]       (fc) {avatar?:String · Gravatar or custom URL};
  \node[eodd,  below=0pt of fc]       (fd) {googleAccessToken?:Short-lived · 1h};
  \node[eeven, below=0pt of fd]       (fe) {googleRefreshToken?:Long-lived · Gmail API};
  \node[eodd,  below=0pt of fe]       (ff) {mutedCategories:String[] · default []};
\end{tikzpicture}
\caption{User Model — Schema Fields}
\end{figure}

\begin{infobox}{Security Notes}
Passwords are hashed with \code{bcryptjs} at 10 salt rounds before storage. \code{googleAccessToken} and \code{googleRefreshToken} are stored in plaintext — in a production environment these should be encrypted at rest using AES-256.
\end{infobox}

\section{Club Model}

\begin{figure}[H]
\centering
\begin{tikzpicture}
  \node[ehead]                   (h)  {Club};
  \node[eodd,  below=0pt of h]   (f1) {clubId:Number · unique · indexed};
  \node[eeven, below=0pt of f1]  (f2) {name:String · required · unique};
  \node[eodd,  below=0pt of f2]  (f3) {description:String · required};
  \node[eeven, below=0pt of f3]  (f4) {category:'Technical' | 'Cultural' | 'Literary' | 'Other'};
  \node[eodd,  below=0pt of f4]  (f5) {followers:Number · default 0};
  \node[eeven, below=0pt of f5]  (f6) {image?:String · Cloudinary CDN URL};
  \node[eodd,  below=0pt of f6]  (f7) {email?:String · Mailgun inbound webhook};
\end{tikzpicture}
\caption{Club Model — Schema Fields}
\end{figure}

\section{Event Model (Global Club Feed)}

\begin{figure}[H]
\centering
\begin{tikzpicture}
  \node[ehead]                   (h)  {Event};
  \node[eodd,  below=0pt of h]   (f1) {clubId:Number · required · indexed};
  \node[eeven, below=0pt of f1]  (f2) {title:String · required};
  \node[eodd,  below=0pt of f2]  (f3) {description?:String};
  \node[eeven, below=0pt of f3]  (f4) {icon:String · emoji · default '📅'};
  \node[eodd,  below=0pt of f4]  (f5) {badge:'LIVE' | 'UPCOMING' · indexed};
  \node[eeven, below=0pt of f5]  (f6) {date:Date · required · indexed · time-embedded};
  \node[eodd,  below=0pt of f6]  (f7) {endDate?:Date · indexed};
  \node[eeven, below=0pt of f7]  (f8) {timeDisplay:String · e.g. "6:00 PM – 8:00 PM"};
  \node[eodd,  below=0pt of f8]  (f9) {location:String · required};
  \node[eeven, below=0pt of f9]  (fa) {imageUrl?:String · Cloudinary CDN URL};
  \node[eodd,  below=0pt of fa]  (fb) {color?:String · hex e.g. '\#CCF900'};
\end{tikzpicture}
\caption{Event Model — Schema Fields. Lifecycle: deleted by cron 1 day after \texttt{event.date}.}
\end{figure}

\section{PersonalEvent Model (Per-User Smart Inbox)}

\begin{figure}[H]
\centering
\begin{tikzpicture}
  \node[ehead]                   (h)  {PersonalEvent};
  \node[eodd,  below=0pt of h]   (f1) {userId:ObjectId · ref: User · indexed};
  \node[eeven, below=0pt of f1]  (f2) {gmailMessageId:String · Gmail/manual ID};
  \node[eodd,  below=0pt of f2]  (f3) {title:String · required};
  \node[eeven, below=0pt of f3]  (f4) {description?:String};
  \node[eodd,  below=0pt of f4]  (f5) {icon:String · emoji};
  \node[eeven, below=0pt of f5]  (f6) {badge:'LIVE' | 'UPCOMING'};
  \node[eodd,  below=0pt of f6]  (f7) {date:Date · required · time-embedded ISO};
  \node[eeven, below=0pt of f7]  (f8) {endDate?:Date · indexed};
  \node[eodd,  below=0pt of f8]  (f9) {timeDisplay:String · human-readable};
  \node[eeven, below=0pt of f9]  (fa) {location:String · required};
  \node[eodd,  below=0pt of fa]  (fb) {color?:String · hex color};
  \node[eeven, below=0pt of fb]  (fc) {sourceFrom:String · email sender};
  \node[eodd,  below=0pt of fc]  (fd) {sourceSubject:String · email subject};
  \node[eeven, below=0pt of fd]  (fe) {reminderSent?:Boolean · 1-hour reminder flag};
  \node[eodd,  below=0pt of fe]  (ff) {category:clubs | interviews | mess | classroom | lost\_found | academic | general | personal};
\end{tikzpicture}
\caption{PersonalEvent Model. Compound unique index: \texttt{\{userId, gmailMessageId\}}. Lifecycle: deleted 28 days after creation.}
\end{figure}

\section{ProcessedEmail Model (AI Parse Cache)}

\begin{figure}[H]
\centering
\begin{tikzpicture}
  \node[ehead]                   (h)  {ProcessedEmail};
  \node[eodd,  below=0pt of h]   (f1) {emailMessageId:String · RFC 2822 ID · unique};
  \node[eeven, below=0pt of f1]  (f2) {isEvent:Boolean · Groq AI decision (cached)};
  \node[eodd,  below=0pt of f2]  (f3) {eventTitle? / eventDescription?:Extracted fields};
  \node[eeven, below=0pt of f3]  (f4) {eventDate? / eventEndDate?:Time-embedded ISO dates};
  \node[eodd,  below=0pt of f4]  (f5) {eventTimeDisplay?:Human-readable time};
  \node[eeven, below=0pt of f5]  (f6) {eventLocation? / eventBadge?:Extracted fields};
  \node[eodd,  below=0pt of f6]  (f7) {eventIcon? / eventColor? / eventCategory?:Styling};
  \node[eeven, below=0pt of f7]  (f8) {processedByUsers:ObjectId[] · \$addToSet};
\end{tikzpicture}
\caption{ProcessedEmail — Deduplication cache. Same email to N users = 1 AI call + N-1 cache hits. Lifecycle: deleted 7 days after creation.}
\end{figure}

\section{Mail \& Category Models}

\begin{figure}[H]
\centering
\begin{tikzpicture}
  % Mail
  \node[ehead, minimum width=5.8cm]                   (mh) {Mail};
  \node[eodd,  minimum width=5.8cm, below=0pt of mh]  (m1) {categoryId:Number · indexed};
  \node[eeven, minimum width=5.8cm, below=0pt of m1]  (m2) {sender / subject / body:required};
  \node[eodd,  minimum width=5.8cm, below=0pt of m2]  (m3) {isRead:Boolean · default false};
  \node[eeven, minimum width=5.8cm, below=0pt of m3]  (m4) {priority:'low' | 'medium' | 'high'};
  \node[eodd,  minimum width=5.8cm, below=0pt of m4]  (m5) {TTL: 7 days auto-delete};

  % Category (to the right)
  \node[ehead, minimum width=5.8cm, right=1cm of mh]                   (ch) {Category};
  \node[eodd,  minimum width=5.8cm, below=0pt of ch]  (c1) {categoryId:Number · unique · indexed};
  \node[eeven, minimum width=5.8cm, below=0pt of c1]  (c2) {name:String · unique};
  \node[eodd,  minimum width=5.8cm, below=0pt of c2]  (c3) {icon:String · Lucide icon name};
  \node[eeven, minimum width=5.8cm, below=0pt of c3]  (c4) {color:String · hex color code};
\end{tikzpicture}
\caption{Mail and Category Models}
\end{figure}

% ─────────────────────────────────────────────────────────────
\chapter{Core Services}
% ─────────────────────────────────────────────────────────────

\section{Gmail Watcher Service}

The Gmail Watcher is the \textbf{central engine} of PulseBoard. It runs as a \code{setInterval} loop every 5 minutes, scanning every registered Google user's Gmail inbox for campus-relevant emails.

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.5cm]
  \node[term]                              (boot)  {App Boot};
  \node[proc=flowblue, below=of boot]      (users) {Find users with\\googleAccessToken + expoPushToken};
  \node[proc=flowblue, below=of users]     (batch) {Process in batches of 10\\(avoid Gmail rate limits)};
  \node[proc=flowblue, below=of batch]     (fetch) {Gmail API: fetch 50\\unread emails (30 days)};
  \node[proc=flowblue, below=of fetch]     (msgid) {Extract RFC 2822\\Message-ID header};
  \node[dec,           below=of msgid]     (cache) {In ProcessedEmail\\cache?};
  \node[proc=floworange, below=of cache]   (miss)  {Cache MISS:\\call Groq API};
  \node[proc=floworange, below=of miss]    (store) {Store result in\\ProcessedEmail cache};
  \node[dec,           below=of store]     (isev)  {isEvent\\= true?};
  \node[proc=flowpurple, below=of isev]    (ups)   {PersonalEvent.upsert()\\for this user};
  \node[proc=flowpurple, below=of ups]     (club)  {If club account:\\Event.create() globally};
  \node[proc=floworange, below=of club]    (push)  {If interviews:\\send push notification};
  \node[proc=flowblue,   below=of push]    (mark)  {\$addToSet: processedByUsers};
  \node[term,            below=of mark]    (wait)  {Wait 5 min · repeat};

  % Main vertical flow
  \draw[arr=darkbg] (boot)--(users);
  \draw[arr=darkbg] (users)--(batch);
  \draw[arr=darkbg] (batch)--(fetch);
  \draw[arr=darkbg] (fetch)--(msgid);
  \draw[arr=darkbg] (msgid)--(cache);
  \draw[arr=darkbg] (cache)  -- node[right,font=\tiny]{NO (MISS)} (miss);
  \draw[arr=darkbg] (miss)  -- (store);
  \draw[arr=darkbg] (store) -- (isev);
  \draw[arr=darkbg] (isev)  -- node[right,font=\tiny]{YES} (ups);
  \draw[arr=darkbg] (ups)   -- (club);
  \draw[arr=darkbg] (club)  -- (push);
  \draw[arr=darkbg] (push)  -- (mark);
  \draw[arr=darkbg] (mark)  -- (wait);

  % Cache HIT: bypass miss+store, go straight to isev
  \draw[arr=darkbg] (cache.east) -- ++(3.2,0)
      node[midway,above,font=\tiny]{YES (HIT)}
      -- ++(0,-3.0) -| (isev.east);

  % isEvent=NO: skip to mark
  \draw[arr=darkbg] (isev.west) -- ++(-3.2,0)
      node[midway,above,font=\tiny]{NO}
      -- ++(0,-3.6) -| (mark.west);
\end{tikzpicture}
\caption{Gmail Watcher Service — Core Processing Loop}
\end{figure}

\section{Email Parser Service}

The Email Parser sends email content to \textbf{Groq's Llama 3.1 8B Instant} model with a carefully engineered prompt that instructs the AI to classify, extract, and structure campus event information.

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                              (in)    {Input: subject + body};
  \node[proc=flowblue, below=of in]        (build) {Build structured prompt\\(rules + JSON schema)};
  \node[proc=floworange, below=of build]   (groq)  {POST to Groq API\\llama-3.1-8b-instant};
  \node[dec, below=of groq]               (ok)    {Response\\OK?};
  \node[proc=flowred, right=2cm of ok]    (fb)    {Fallback:\\smartRegexParse()};
  \node[proc=flowblue, below=of ok]        (parse) {JSON.parse()\\response content};
  \node[dec, below=of parse]              (isev)  {isEvent\\= true?};
  \node[proc=flowred, right=2cm of isev]  (null)  {Return null\\(skip email)};
  \node[proc=flowgreen, below=of isev]    (ret)   {Return ParsedEvent\\\{title, date, location,\\timeDisplay, category,\\icon, color, badge\}};

  \draw[arr=darkbg] (in)--(build);
  \draw[arr=darkbg] (build)--(groq);
  \draw[arr=darkbg] (groq)--(ok);
  \draw[arr=darkbg] (ok)  -- node[above,font=\tiny]{FAIL} (fb);
  \draw[arr=darkbg] (ok)  -- node[right,font=\tiny]{OK} (parse);
  \draw[arr=darkbg] (parse)--(isev);
  \draw[arr=darkbg] (isev) -- node[above,font=\tiny]{NO} (null);
  \draw[arr=darkbg] (isev) -- node[right,font=\tiny]{YES} (ret);
  \draw[arr=darkbg] (fb.south) |- (isev.east);
\end{tikzpicture}
\caption{Email Parser Service — AI Parsing Flow}
\end{figure}

\subsection{AI Prompt Design}

The prompt instructs the LLM with explicit rules for:

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\bfseries}l X}
\rowcolor{tableheadbg}
\color{white}Rule & \color{white}Detail \\
\toprule
INCLUDE          & Club events, fests, hackathons, workshops, seminars \\
INCLUDE          & Interview/placement drives from companies or college \\
INCLUDE          & Exam schedules, deadlines, academic requirements     \\
INCLUDE          & Sports events, scholarships, conferences             \\
BLOCK            & OTPs, password resets, login alerts                 \\
BLOCK            & E-commerce: Myntra, Amazon, Flipkart, Nykaa, Meesho \\
BLOCK            & Sale offers, discount codes, order confirmations     \\
BLOCK            & Social media notifications, brand promotions         \\
Time Rule        & Embed exact time in ISO date (not midnight!)         \\
Time Rule        & Deadlines default to 11:59 PM                       \\
Time Rule        & Resolve ``tomorrow'', ``next Monday'' from today's date \\
\bottomrule
\end{tabularx}
\caption{AI Prompt Classification Rules}
\end{table}

\subsection{Category \& Color Mapping}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{l l l X}
\rowcolor{tableheadbg}
\color{white}Category & \color{white}Icon & \color{white}Color & \color{white}Matches \\
\toprule
clubs          & \texttt{🎭} & \texttt{\#F59E0B} & Club events, fests, cultural \\
interviews     & \texttt{💼} & \texttt{\#F97316} & Placement drives, HR rounds  \\
academic       & \texttt{📚} & \texttt{\#3B82F6} & Exams, timetables, labs      \\
general        & \texttt{📢} & \texttt{\#6B7280} & Other campus-relevant emails \\
mess           & \texttt{🍽}  & \texttt{\#10B981} & Mess menu, canteen notices   \\
google\_classroom & \texttt{📋} & \texttt{\#4285F4} & Classroom assignments        \\
lost\_found    & \texttt{🔍} & \texttt{\#8B5CF6} & Lost \& found notices        \\
personal       & \texttt{👤} & \texttt{\#CCF900} & Manually created events      \\
\bottomrule
\end{tabularx}
\caption{Event Category Mappings}
\end{table}

\subsection{Fallback Parser}

If the Groq API is unavailable (429 / timeout), \code{smartRegexParse()} activates:

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                               (in)   {Input: subject + body};
  \node[proc=flowblue, below=of in]         (low)  {Lowercase full text};
  \node[dec, below=of low]                  (spam) {Contains spam\\keyword?};
  \node[proc=flowred, right=2.5cm of spam]  (no)   {Return null};
  \node[dec, below=of spam]                 (evt)  {Contains event\\keyword?};
  \node[proc=flowred, right=2.5cm of evt]   (no2)  {Return null};
  \node[proc=flowblue, below=of evt]        (time) {Regex: extract\\time (HH:MM AM/PM)};
  \node[proc=flowblue, below=of time]       (date) {Regex: extract date\\(DD/MM/YYYY or Mon DD)};
  \node[proc=flowgreen, below=of date]      (ret)  {buildEventFromRegex()\\return ParsedEvent};

  \draw[arr=darkbg] (in)--(low);
  \draw[arr=darkbg] (low)--(spam);
  \draw[arr=darkbg] (spam) -- node[above,font=\tiny]{YES} (no);
  \draw[arr=darkbg] (spam) -- node[right,font=\tiny]{NO} (evt);
  \draw[arr=darkbg] (evt)  -- node[above,font=\tiny]{NO} (no2);
  \draw[arr=darkbg] (evt)  -- node[right,font=\tiny]{YES} (time);
  \draw[arr=darkbg] (time)--(date);
  \draw[arr=darkbg] (date)--(ret);
\end{tikzpicture}
\caption{Regex-based Fallback Parser Flow}
\end{figure}

\section{Reminder Scheduler Service}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                               (boot) {App Boot};
  \node[proc=flowblue, below=of boot]       (q)    {PersonalEvent.find()\\date in [now+55min, now+65min]\\reminderSent = false};
  \node[dec, below=of q]                    (any)  {Events\\found?};
  \node[proc=floworange, below=of any]      (each) {For each event:\\look up user.expoPushToken};
  \node[dec, below=of each]                 (tok)  {Token\\exists?};
  \node[proc=flowgreen, below=of tok]       (push) {sendPushNotification()\\"\{icon\} Starting in 1 hour!"};
  \node[proc=flowblue, below=of push]       (mark) {PersonalEvent.update()\\reminderSent = true};
  \node[term, below=of mark]                (wait) {Wait 5 min · repeat};

  \draw[arr=darkbg] (boot)--(q);
  \draw[arr=darkbg] (q)--(any);
  \draw[arr=darkbg] (any) -- node[right,font=\tiny]{YES} (each);
  \draw[arr=darkbg] (any.west) -- ++(-1,0) node[left,font=\tiny]{NO} |- (wait.west);
  \draw[arr=darkbg] (each)--(tok);
  \draw[arr=darkbg] (tok) -- node[right,font=\tiny]{YES} (push);
  \draw[arr=darkbg] (tok.east) -- ++(1,0) node[right,font=\tiny]{NO} |- (wait.east);
  \draw[arr=darkbg] (push)--(mark);
  \draw[arr=darkbg] (mark)--(wait);
\end{tikzpicture}
\caption{Reminder Scheduler — 5-minute polling loop with 10-minute detection window (55–65 min)}
\end{figure}

\section{LHC Service}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  % isLHCRoomAvailable
  \node[term, minimum width=5cm]               (in1)  {isLHCRoomAvailable(room, start, end)};
  \node[proc=flowblue, below=of in1]           (q1)   {Event.findOne()\\location~=LHC\{room\}\\date < end AND endDate > start};
  \node[dec, below=of q1]                      (d1)   {Conflict\\found?};
  \node[proc=flowred,  right=2.5cm of d1]      (r1)   {\{ available: false,\\  conflict: event \}};
  \node[proc=flowgreen, below=of d1]           (r2)   {\{ available: true \}};

  \draw[arr=darkbg] (in1)--(q1);
  \draw[arr=darkbg] (q1)--(d1);
  \draw[arr=darkbg] (d1) -- node[above,font=\tiny]{YES} (r1);
  \draw[arr=darkbg] (d1) -- node[right,font=\tiny]{NO}  (r2);

  % getLHCHeatmap — placed below
  \node[term, minimum width=5cm, below=2.8cm of r2]  (in2)  {getLHCHeatmap(date)};
  \node[proc=flowblue, below=of in2]                 (q2)   {Event.find()\\date in [dayStart, dayEnd]\\location contains 'LHC'};
  \node[proc=flowgreen, below=of q2]                 (r3)   {Map to LHCBooking[]\\room, title, start, end, type, color};

  \draw[arr=darkbg] (in2)--(q2);
  \draw[arr=darkbg] (q2)--(r3);
\end{tikzpicture}
\caption{LHC Service — Room availability check (top) and heatmap query (bottom)}
\end{figure}

\section{Push Notification Service}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                                (in)   {sendPushNotification(token, title, body)};
  \node[dec, below=of in]                    (val)  {Token format\\valid?};
  \node[proc=flowred, right=2.5cm of val]   (warn) {console.warn()\\return early};
  \node[proc=flowblue, below=of val]         (post) {POST to Expo Push API\\https://exp.host/--/api/v2/push/send};
  \node[proc=flowgreen, below=of post]       (ok)   {Notification delivered\\to user's device};

  \draw[arr=darkbg] (in)--(val);
  \draw[arr=darkbg] (val) -- node[above,font=\tiny]{INVALID} (warn);
  \draw[arr=darkbg] (val) -- node[right,font=\tiny]{VALID}   (post);
  \draw[arr=darkbg] (post)--(ok);
\end{tikzpicture}
\caption{Push Notification Service — Expo Push API delivery}
\end{figure}

\section{Cron Jobs}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm]
  \node[term, minimum width=6cm]           (t)  {Daily Cron — 00:00 midnight};
  \node[proc=flowred, below=of t]          (j1) {Delete global Events\\older than 1 day};
  \node[proc=floworange, below=of j1]      (j2) {Delete PersonalEvents\\older than 28 days};
  \node[proc=flowpurple, below=of j2]      (j3) {Delete ProcessedEmail cache\\older than 7 days};
  \node[proc=flowblue, below=of j3]        (log){console.log()\\deleted counts for each};

  \draw[arr=darkbg] (t)--(j1);
  \draw[arr=darkbg] (j1)--(j2);
  \draw[arr=darkbg] (j2)--(j3);
  \draw[arr=darkbg] (j3)--(log);

  % annotation
  \node[right=0.8cm of j1, font=\footnotesize, text=flowred!70, align=left]
    {Keeps feed clean\\post-event};
  \node[right=0.8cm of j2, font=\footnotesize, text=floworange!70, align=left]
    {Long retention for\\inbox history};
  \node[right=0.8cm of j3, font=\footnotesize, text=flowpurple!70, align=left]
    {Forces re-analysis of\\recurring emails monthly};
\end{tikzpicture}
\caption{Cron Jobs — Data Retention Policy (node-cron schedule: \texttt{0 0 * * *})}
\end{figure}

% ─────────────────────────────────────────────────────────────
\chapter{Mobile Application}
% ─────────────────────────────────────────────────────────────

\section{Navigation Architecture}

PulseBoard uses \textbf{Expo Router} (file-based routing) — the file structure directly defines the navigation graph.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\footnotesize},
  level 1/.style={sibling distance=3.8cm, level distance=1.2cm},
  level 2/.style={sibling distance=1.8cm, level distance=1.1cm},
  edge from parent/.style={draw, -Stealth, gray!60},
]
\node[troot] {app/}
  child { node[tnode] {index.tsx\\(Welcome)} }
  child { node[tnode] {auth/}
    child { node[tnode, minimum width=1.6cm] {login} }
    child { node[tnode, minimum width=1.6cm] {register} }
    child { node[tnode, minimum width=1.6cm] {verify-otp} }
  }
  child { node[tnode] {tabs/}
    child { node[tnode, minimum width=1.4cm] {home} }
    child { node[tnode, minimum width=1.4cm] {clubs} }
    child { node[tnode, minimum width=1.4cm] {inbox} }
    child { node[tnode, minimum width=1.4cm] {profile} }
  }
  child { node[tnode] {club\_tabs/}
    child { node[tnode, minimum width=1.4cm] {home} }
    child { node[tnode, minimum width=1.4cm] {posts} }
  }
  child { node[tnode] {calendar\\heatmap\\sos\\settings} };
\end{tikzpicture}
\caption{Expo Router Navigation Tree — file path = route path}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                              (boot)  {App Launch};
  \node[proc=flowblue, below=of boot]      (reg)   {registerForPushNotifications()\\save token to AsyncStorage};
  \node[proc=flowblue, below=of reg]       (post)  {POST /users/save-push-token\\(background, non-blocking)};
  \node[proc=flowpurple, below=of post]    (wrap)  {Wrap children in\\ThemeProvider};
  \node[proc=flowblue, below=of wrap]      (stack) {Render Stack Navigator\\screenOptions: headerShown=false};
  \node[proc=flowgreen, below=of stack]    (scr)   {Register screens:\\index, auth, tabs, calendar,\\settings, sos, heatmap};

  \draw[arr=darkbg] (boot)--(reg);
  \draw[arr=darkbg] (reg)--(post);
  \draw[arr=darkbg] (post)--(wrap);
  \draw[arr=darkbg] (wrap)--(stack);
  \draw[arr=darkbg] (stack)--(scr);
\end{tikzpicture}
\caption{app/\_layout.tsx — Root Navigator Initialization}
\end{figure}

\section{API Client Layer}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm]
  \node[proc=flowblue, minimum width=6cm]     (ax)   {axios.create()\\baseURL: EXPO\_PUBLIC\_API\_URL/api\\timeout: 15000ms};
  \node[proc=floworange, below=of ax]         (int)  {Request Interceptor\\reads JWT from AsyncStorage};
  \node[dec, below=of int]                    (tok)  {Token\\exists?};
  \node[proc=flowgreen, below left=0.7cm and 1.5cm of tok]   (yes)  {Attach header:\\Authorization: Bearer \{token\}};
  \node[proc=flowred, below right=0.7cm and 1.5cm of tok]   (no)   {Forward request\\unauthenticated};
  \node[proc=flowblue, below=2cm of tok]      (out)  {Request sent to backend};

  \draw[arr=darkbg] (ax)--(int);
  \draw[arr=darkbg] (int)--(tok);
  \draw[arr=darkbg] (tok) -- node[above left,font=\tiny]{YES} (yes);
  \draw[arr=darkbg] (tok) -- node[above right,font=\tiny]{NO} (no);
  \draw[arr=darkbg] (yes.south) |- (out.west);
  \draw[arr=darkbg] (no.south)  |- (out.east);
\end{tikzpicture}
\caption{src/api/client.ts — Axios instance with automatic JWT injection}
\end{figure}

\section{Screen Breakdown}

\subsection{Welcome Screen (\texttt{index.tsx})}

The splash/welcome screen that auto-redirects authenticated users to \code{/tabs/home}:

\begin{itemize}[leftmargin=*]
  \item On mount: checks \code{AsyncStorage} for JWT token
  \item If token found: \code{router.replace('/tabs/home')}
  \item If no token: renders welcome UI with Sign Up and Login buttons
  \item Design: Dark ``PB'' logo with volt yellow accent, skewed typography
\end{itemize}

\subsection{Home Screen (\texttt{tabs/home.tsx})}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                                (start) {Screen Mount};
  \node[proc=flowblue, below=of start]       (par)   {Promise.all() — 4 parallel requests};

  \node[procS=flowblue,  below left=0.8cm and 3.5cm of par]  (u) {GET /users/me\\user profile};
  \node[procS=flowblue,  below left=0.8cm and 1.0cm of par]  (e) {GET /events/feed\\club events};
  \node[procS=flowblue,  below right=0.8cm and 1.0cm of par] (c) {GET /clubs\\all clubs};
  \node[procS=flowblue,  below right=0.8cm and 3.5cm of par] (p) {GET /personal-events\\inbox events};

  \node[proc=flowpurple, below=2.2cm of par]  (join) {Merge results};
  \node[proc=floworange, below=of join]        (adm)  {Check if user is club admin\\(email match in clubs list)};
  \node[proc=flowgreen,  below=of adm]         (rend) {Render: Happening Now\\Coming Up · Interview alerts};

  \draw[arr=darkbg] (start)--(par);
  \draw[arr=darkbg] (par.south) -- ++(0,-0.3) -| (u.north);
  \draw[arr=darkbg] (par.south) -- ++(0,-0.3) -| (e.north);
  \draw[arr=darkbg] (par.south) -- ++(0,-0.3) -| (c.north);
  \draw[arr=darkbg] (par.south) -- ++(0,-0.3) -| (p.north);
  \draw[arr=darkbg] (u.south) |- (join.west);
  \draw[arr=darkbg] (e.south) |- (join.west);
  \draw[arr=darkbg] (c.south) |- (join.east);
  \draw[arr=darkbg] (p.south) |- (join.east);
  \draw[arr=darkbg] (join)--(adm);
  \draw[arr=darkbg] (adm)--(rend);
\end{tikzpicture}
\caption{Home Screen — Parallel data loading with Promise.all()}
\end{figure}

\textbf{Features:}
\begin{itemize}[leftmargin=*]
  \item \textbf{Happening Now} — horizontally scrollable live event cards for followed clubs
  \item \textbf{Coming Up} — chronologically sorted upcoming events + interview alerts
  \item \textbf{Sidebar Menu} — slide-in panel with: LHC Heatmap, S.O.S., Settings, Logout
  \item \textbf{Admin Panel} — if logged-in user is a club admin, shows ``Publish Event'' modal
  \item \textbf{Event Detail Modal} — full bottom sheet with image, description, date, location
\end{itemize}

\subsection{Smart Inbox Screen (\texttt{tabs/inbox.tsx})}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.5cm]
  % Category grid
  \foreach \cat/\ico/\col/\x/\y in {
    personal/👤/flowblue/0/0,
    clubs/🎯/floworange/2.6/0,
    interviews/💼/flowred/5.2/0,
    mess/🍽/flowgreen/7.8/0,
    classroom/📚/flowblue/0/-1.1,
    lost\_found/🔍/flowpurple/2.6/-1.1,
    academic/🎓/flowblue/5.2/-1.1,
    general/📌/type/7.8/-1.1
  }{
    \node[rectangle, rounded corners=4pt, draw=\col!60, fill=\col!10,
          minimum width=2.3cm, minimum height=0.85cm, align=center,
          font=\small, inner sep=3pt]
      at (\x,\y) {\ico\\\textbf{\cat}};
  }
  % label
  \node[above=0.5cm, font=\small\bfseries, text=tableheadbg]
    at (3.9, 0.5) {8 Categories (Personal = default for manual events)};
  % mute note
  \node[below=0.3cm, font=\footnotesize, text=gray, align=center]
    at (3.9, -1.9) {Muted categories excluded server-side via \$nin · shown dimmed with 🔕 on filter chips};
\end{tikzpicture}
\caption{Smart Inbox — Category Map}
\end{figure}

\textbf{User-facing features:}
\begin{itemize}[leftmargin=*]
  \item \textbf{Category filter chips} — horizontal scroll row; muted categories shown dimmed with \texttt{🔕}
  \item \textbf{Delete} — trash icon on each card; ownership enforced server-side with \texttt{findOneAndDelete(\{\_id, userId\})}
  \item \textbf{Add Event FAB} — \texttt{+} button (bottom-right) opens a create modal; defaults category to \textit{Personal}; user can switch before saving
  \item \textbf{Category Mute Sheet} — sliders icon in header opens a bottom sheet with a \texttt{Switch} per category; preferences persisted to \texttt{User.mutedCategories}
  \item \textbf{Re-scan} — clears server cache and triggers fresh Gmail analysis within 5 minutes
\end{itemize}

\textbf{Event Detail Modal — Hero Design:}
\begin{itemize}[leftmargin=*]
  \item \textbf{Hero banner} — tinted with the event's accent color; decorative background blobs; large emoji centered; category badge and LIVE badge overlaid
  \item \textbf{Action bar} — close (X) and Delete float inside the hero header
  \item \textbf{Info chips} — date, time, and location rendered as individual pill chips, each with a color-matched icon
  \item \textbf{Source card} — inset card showing the originating email sender and subject
\end{itemize}

\subsection{Clubs Screen (\texttt{tabs/clubs.tsx})}

\begin{itemize}[leftmargin=*]
  \item 2-column grid of club cards with emoji icons
  \item Live search by name and description
  \item Category filter tabs (Technical / Cultural / Literary / Other)
  \item Follow/unfollow with optimistic UI update
  \item Stats strip: total clubs count + user's following count
\end{itemize}

\subsection{Calendar Screen (\texttt{calendar.tsx})}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                                (in)   {Input: timeDisplay string\\e.g. "6:00 PM – 8:00 PM"};
  \node[dec, below=of in]                    (rng)  {Time range\\regex match?};
  \node[proc=flowgreen, right=2.5cm of rng] (r1)   {Return \{start, end\}\\from both times};
  \node[dec, below=of rng]                   (sngl) {Single time\\regex match?};
  \node[proc=flowgreen, right=2.5cm of sngl](r2)   {Return \{start,\\end=start+1h\}};
  \node[proc=floworange, below=of sngl]      (def)  {Default fallback:\\9 AM – 10 AM};

  \draw[arr=darkbg] (in)--(rng);
  \draw[arr=darkbg] (rng)  -- node[above,font=\tiny]{YES} (r1);
  \draw[arr=darkbg] (rng)  -- node[right,font=\tiny]{NO}  (sngl);
  \draw[arr=darkbg] (sngl) -- node[above,font=\tiny]{YES} (r2);
  \draw[arr=darkbg] (sngl) -- node[right,font=\tiny]{NO}  (def);
\end{tikzpicture}
\caption{Calendar — \texttt{parseTimeDisplay()} logic for converting human-readable times to ISO dates}
\end{figure}

\subsection{LHC Heatmap Screen (\texttt{heatmap.tsx})}

\begin{itemize}[leftmargin=*]
  \item Rooms tracked: \texttt{110, 205, 206, 305, 306, 308}
  \item Time grid: 8 AM – 9 PM in hourly rows
  \item Real-time occupancy status cards (animated pulsing dot)
  \item Horizontal + vertical scroll for full timeline view
  \item Date navigation (previous/next day)
  \item Color-coded blocks: club events vs personal events
  \item Double-booking prevention via \code{isLHCRoomAvailable()}
\end{itemize}

\subsection{SOS Screen (\texttt{sos.tsx})}

\begin{itemize}[leftmargin=*]
  \item Emergency contact cards with one-tap \code{Linking.openURL('tel:...')}
  \item Sections: Medical, Campus Security, Counseling, Important Links
  \item Red accent theme with \code{HeartPulse} and \code{AlertTriangle} icons
  \item Platform-specific (iOS/Android) phone call handling
\end{itemize}

\section{Theme Context}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm]
  \node[proc=flowblue, minimum width=6cm]   (tp)   {ThemeProvider mounts};
  \node[proc=floworange, below=of tp]       (set)  {useEffect():\\setColorScheme('dark')\\via NativeWind};
  \node[proc=flowpurple, below=of set]      (ctx)  {ThemeContext.Provider\\value: \{ isDark: true \}};
  \node[proc=flowgreen, below=of ctx]       (use)  {Any component: useTheme()\\→ isDark: true/false};

  \draw[arr=darkbg] (tp)--(set);
  \draw[arr=darkbg] (set)--(ctx);
  \draw[arr=darkbg] (ctx)--(use);
\end{tikzpicture}
\caption{ThemeContext — Dark mode locked globally via NativeWind \texttt{setColorScheme}}
\end{figure}

\begin{warnbox}{Important: darkMode config}
\code{tailwind.config.js} must use \texttt{darkMode: "media"} (not \texttt{"class"}) for NativeWind's \code{setColorScheme()} to activate \texttt{dark:} prefixed classes. Using \texttt{"class"} mode requires a DOM-level \texttt{.dark} parent class which doesn't exist in React Native.
\end{warnbox}

% ─────────────────────────────────────────────────────────────
\chapter{Data Flows \& Sequence Diagrams}
% ─────────────────────────────────────────────────────────────

\section{User Registration Flow}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  actor/.style={rectangle, rounded corners=3pt, draw=tableheadbg,
                fill=tableheadbg, text=white, font=\small\bfseries,
                minimum width=2.3cm, minimum height=0.65cm, align=center},
  smsg/.style={-{Stealth}, thick},
  sret/.style={-{Stealth}, thick, dashed, gray},
  sll/.style={dashed, gray!40, line width=0.4pt},
]
  % Actors
  \node[actor] (M)  at (0,0)    {Mobile};
  \node[actor] (B)  at (4.5,0)  {Backend};
  \node[actor] (DB) at (9,0)    {MongoDB};
  \node[actor] (SM) at (12.5,0) {SMTP};

  % Lifelines
  \foreach \a in {M,B,DB,SM}
    \draw[sll] (\a.south) -- ++(0,-9.5);

  % Messages
  \draw[smsg] (0,-0.8) -- node[above,font=\tiny]{POST /register \{name, email, password\}} (4.5,-0.8);
  \draw[smsg] (4.5,-1.5) -- node[above,font=\tiny]{bcrypt.hash(password)} (9,-1.5);
  \draw[smsg] (4.5,-2.2) -- node[above,font=\tiny]{User.create()} (9,-2.2);
  \draw[sret] (9,-2.9)   -- node[above,font=\tiny]{user saved} (4.5,-2.9);
  \draw[smsg] (4.5,-3.6) -- node[above,font=\tiny]{sendOtpEmail()} (12.5,-3.6);
  \draw[sret] (4.5,-4.3) -- node[above,font=\tiny]{201 \{email\}} (0,-4.3);

  \node[font=\tiny, right=0.1cm] at (0,-4.3) {};
  \draw[smsg] (0,-5.0) -- node[above,font=\tiny]{POST /verify-otp \{email, otp\}} (4.5,-5.0);
  \draw[smsg] (4.5,-5.7) -- node[above,font=\tiny]{validateOTP() + markVerified()} (9,-5.7);
  \draw[sret] (9,-6.4)  -- node[above,font=\tiny]{updated} (4.5,-6.4);
  \draw[sret] (4.5,-7.1)-- node[above,font=\tiny]{200 \{token: JWT\}} (0,-7.1);
  \draw[smsg] (0,-7.8)  -- node[above,font=\tiny]{AsyncStorage.setItem('token') + POST /save-push-token} (4.5,-7.8);
\end{tikzpicture}
\caption{User Registration \& Verification Sequence}
\end{figure}

\section{Gmail Email \textrightarrow{} Smart Inbox Flow}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  actor/.style={rectangle, rounded corners=3pt, draw=tableheadbg,
                fill=tableheadbg, text=white, font=\small\bfseries,
                minimum width=2.3cm, minimum height=0.65cm, align=center},
  smsg/.style={-{Stealth}, thick},
  sret/.style={-{Stealth}, thick, dashed, gray},
  sll/.style={dashed, gray!40, line width=0.4pt},
]
  \node[actor] (GW) at (0,0)    {GmailWatcher};
  \node[actor] (DB) at (4,0)    {MongoDB};
  \node[actor] (GR) at (8,0)    {Groq API};
  \node[actor] (GL) at (12,0)   {Gmail API};

  \foreach \a in {GW,DB,GR,GL}
    \draw[sll] (\a.south) -- ++(0,-11);

  \draw[smsg] (0,-0.8)  -- node[above,font=\tiny]{fetch users with token + pushToken} (4,-0.8);
  \draw[sret] (4,-1.5)  -- node[above,font=\tiny]{users[]} (0,-1.5);
  \draw[smsg] (0,-2.2)  -- node[above,font=\tiny]{GET /messages (50 unread, 30d)} (12,-2.2);
  \draw[sret] (12,-2.9) -- node[above,font=\tiny]{messages[]} (0,-2.9);
  \draw[smsg] (0,-3.6)  -- node[above,font=\tiny]{ProcessedEmail.findOne(messageId)} (4,-3.6);
  \draw[sret] (4,-4.3)  -- node[above,font=\tiny]{null (MISS)} (0,-4.3);
  \draw[smsg] (0,-5.0)  -- node[above,font=\tiny]{parseEventFromEmail(subject, body)} (8,-5.0);
  \draw[sret] (8,-5.7)  -- node[above,font=\tiny]{ParsedEvent JSON} (0,-5.7);
  \draw[smsg] (0,-6.4)  -- node[above,font=\tiny]{ProcessedEmail.create()} (4,-6.4);

  \node[font=\tiny\itshape, right=0.2cm] at (0,-7.1) {[Cache HIT path: skip Groq, reuse cached result]};

  \draw[smsg] (0,-7.8)  -- node[above,font=\tiny]{PersonalEvent.upsert()} (4,-7.8);
  \draw[smsg] (0,-8.5)  -- node[above,font=\tiny]{if club: Event.create()} (4,-8.5);
  \draw[smsg] (0,-9.2)  -- node[above,font=\tiny]{if interviews: sendPushNotification()} (4,-9.2);
  \draw[smsg] (0,-9.9)  -- node[above,font=\tiny]{\$addToSet: processedByUsers} (4,-9.9);
\end{tikzpicture}
\caption{Gmail Email Parsing Pipeline — GmailWatcher to Smart Inbox}
\end{figure}

\section{Event Creation Flow (Club Admin)}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  actor/.style={rectangle, rounded corners=3pt, draw=tableheadbg,
                fill=tableheadbg, text=white, font=\small\bfseries,
                minimum width=2.5cm, minimum height=0.65cm, align=center},
  smsg/.style={-{Stealth}, thick},
  sret/.style={-{Stealth}, thick, dashed, gray},
  sll/.style={dashed, gray!40, line width=0.4pt},
]
  \node[actor] (M)  at (0,0)   {Mobile (Admin)};
  \node[actor] (B)  at (4.5,0) {Backend};
  \node[actor] (CL) at (9,0)   {Cloudinary};
  \node[actor] (DB) at (12.5,0){MongoDB};

  \foreach \a in {M,B,CL,DB}
    \draw[sll] (\a.south) -- ++(0,-9);

  \draw[smsg] (0,-0.8) -- node[above,font=\tiny]{POST /events (FormData: title, date, image…)} (4.5,-0.8);
  \draw[smsg] (4.5,-1.5) -- node[above,font=\tiny]{upload image} (9,-1.5);
  \draw[sret] (9,-2.2)  -- node[above,font=\tiny]{imageUrl} (4.5,-2.2);
  \draw[smsg] (4.5,-2.9) -- node[above,font=\tiny]{isLHCRoomAvailable(room, start, end)} (12.5,-2.9);
  \draw[sret] (12.5,-3.6)-- node[above,font=\tiny]{available: true} (4.5,-3.6);
  \draw[smsg] (4.5,-4.3) -- node[above,font=\tiny]{Event.create()} (12.5,-4.3);
  \draw[smsg] (4.5,-5.0) -- node[above,font=\tiny]{get club followers} (12.5,-5.0);
  \draw[sret] (12.5,-5.7)-- node[above,font=\tiny]{followers[]} (4.5,-5.7);
  \draw[smsg] (4.5,-6.4) -- node[above,font=\tiny]{sendPushNotification() × N followers} (12.5,-6.4);
  \draw[sret] (4.5,-7.8) -- node[above,font=\tiny]{201 \{event\}} (0,-7.8);
\end{tikzpicture}
\caption{Club Admin Event Publishing — Image upload, conflict check, fan-out notifications}
\end{figure}

\section{1-Hour Reminder Flow}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  actor/.style={rectangle, rounded corners=3pt, draw=tableheadbg,
                fill=tableheadbg, text=white, font=\small\bfseries,
                minimum width=2.8cm, minimum height=0.65cm, align=center},
  smsg/.style={-{Stealth}, thick},
  sret/.style={-{Stealth}, thick, dashed, gray},
  sll/.style={dashed, gray!40, line width=0.4pt},
]
  \node[actor] (RS) at (0,0)   {ReminderScheduler};
  \node[actor] (DB) at (5.5,0) {MongoDB};
  \node[actor] (EX) at (11,0)  {Expo Push API};

  \foreach \a in {RS,DB,EX}
    \draw[sll] (\a.south) -- ++(0,-7.5);

  \draw[smsg] (0,-0.8) -- node[above,font=\tiny]{PersonalEvent.find(\{date: now+55m..now+65m, reminderSent:false\})} (5.5,-0.8);
  \draw[sret] (5.5,-1.6) -- node[above,font=\tiny]{[event1, event2, …]} (0,-1.6);
  \draw[smsg] (0,-2.4) -- node[above,font=\tiny]{for each: sendPushNotification(token, title, body)} (11,-2.4);
  \draw[sret] (11,-3.2) -- node[above,font=\tiny]{200 OK} (0,-3.2);
  \draw[smsg] (0,-4.0) -- node[above,font=\tiny]{PersonalEvent.update(\{reminderSent: true\})} (5.5,-4.0);
  \node[font=\footnotesize\itshape, right=0.5cm, align=left] at (0,-5.5)
    {10-minute detection window (55–65 min)\\absorbs scheduler timing drift.};
\end{tikzpicture}
\caption{1-Hour Pre-Event Push Reminder Sequence}
\end{figure}

% ─────────────────────────────────────────────────────────────
\chapter{Authentication \& Security}
% ─────────────────────────────────────────────────────────────

\section{JWT Authentication}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[term]                              (req)  {Incoming Request};
  \node[dec, below=of req]                 (hdr)  {Authorization\\header present?};
  \node[proc=flowred, right=2.5cm of hdr] (e1)   {401 No token\\provided};
  \node[dec, below=of hdr]                 (fmt)  {Starts with\\Bearer?};
  \node[proc=flowred, right=2.5cm of fmt] (e2)   {401 Malformed\\token};
  \node[proc=flowblue, below=of fmt]       (ver)  {jwt.verify(token,\\JWT\_SECRET)};
  \node[dec, below=of ver]                 (ok)   {Valid?};
  \node[proc=flowred, right=2.5cm of ok]  (e3)   {401 Invalid or\\expired token};
  \node[proc=flowgreen, below=of ok]       (att)  {Attach decoded payload\\to req.user};
  \node[term, below=of att]                (nxt)  {next() — continue\\to controller};

  \draw[arr=darkbg] (req)--(hdr);
  \draw[arr=darkbg] (hdr) -- node[above,font=\tiny]{NO} (e1);
  \draw[arr=darkbg] (hdr) -- node[right,font=\tiny]{YES} (fmt);
  \draw[arr=darkbg] (fmt) -- node[above,font=\tiny]{NO} (e2);
  \draw[arr=darkbg] (fmt) -- node[right,font=\tiny]{YES} (ver);
  \draw[arr=darkbg] (ver)--(ok);
  \draw[arr=darkbg] (ok) -- node[above,font=\tiny]{NO} (e3);
  \draw[arr=darkbg] (ok) -- node[right,font=\tiny]{YES} (att);
  \draw[arr=darkbg] (att)--(nxt);
\end{tikzpicture}
\caption{auth.middleware.ts — JWT verification flow. Token expiry: 7 days · Algorithm: HS256}
\end{figure}

\section{Google OAuth Flow}

PulseBoard supports two distinct OAuth flows depending on the client:

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\bfseries}l X X}
\rowcolor{tableheadbg}
\color{white}Flow & \color{white}Mobile (Expo) & \color{white}Web OAuth \\
\toprule
Token Type   & \texttt{id\_token} (verified directly)       & \texttt{code} exchanged for tokens \\
Refresh Token & Not provided (implicit flow)                & Yes — stored for Gmail API \\
Use Case     & Basic login, identity verification          & Gmail inbox access \\
Library      & \texttt{google-auth-library} verifyIdToken  & \texttt{getToken(code, redirectUri)} \\
\bottomrule
\end{tabularx}
\caption{Google OAuth Flow Comparison}
\end{table}

\section{Password Security}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm]

  % Registration path
  \node[term, minimum width=5cm]               (reg)  {Registration};
  \node[proc=flowblue, below=of reg]           (gs)   {bcrypt.genSalt(10)};
  \node[proc=flowblue, below=of gs]            (hash) {bcrypt.hash(password, salt)};
  \node[proc=flowgreen, below=of hash]         (save) {store hashed password in DB};

  \draw[arr=darkbg] (reg)--(gs);
  \draw[arr=darkbg] (gs)--(hash);
  \draw[arr=darkbg] (hash)--(save);

  % Login path (to the right)
  \node[term, minimum width=5cm, right=2.5cm of reg]         (login){Login};
  \node[proc=flowblue, below=of login]         (cmp)  {bcrypt.compare(plain, hashed)};
  \node[dec, below=of cmp]                     (chk)  {Match?};
  \node[proc=flowgreen, below left=0.7cm and 1cm of chk]   (jwt)  {Sign JWT\\return token};
  \node[proc=flowred, below right=0.7cm and 1cm of chk]   (rej)  {401 Invalid\\credentials};

  \draw[arr=darkbg] (login)--(cmp);
  \draw[arr=darkbg] (cmp)--(chk);
  \draw[arr=darkbg] (chk) -- node[left,font=\tiny]{YES} (jwt);
  \draw[arr=darkbg] (chk) -- node[right,font=\tiny]{NO} (rej);

\end{tikzpicture}
\caption{Password security — bcryptjs at 10 salt rounds. OTP validity: 10 min (registration), 15 min (reset).}
\end{figure}

% ─────────────────────────────────────────────────────────────
\chapter{Configuration \& Deployment}
% ─────────────────────────────────────────────────────────────

\section{Environment Variables}

\subsection{Backend (\texttt{pulseboard\_backend/.env})}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\ttfamily\small}l X}
\rowcolor{tableheadbg}
\color{white}\normalfont Variable & \color{white}Purpose \\
\toprule
MONGO\_URI              & MongoDB Atlas connection string \\
JWT\_SECRET             & HS256 signing key (min 32 chars) \\
ADMIN\_SEED\_KEY        & Seed key for admin account creation \\
GOOGLE\_CLIENT\_ID      & Web OAuth 2.0 client ID \\
GOOGLE\_CLIENT\_SECRET  & Web OAuth 2.0 client secret \\
GOOGLE\_REDIRECT\_URI   & OAuth callback URL \\
GOOGLE\_MOBILE\_CLIENT\_ID & Expo/mobile OAuth client ID \\
GMAIL\_APP\_PASSWORD    & App-specific Gmail SMTP password \\
SENDGRID\_API\_KEY      & SendGrid transactional email \\
SENDGRID\_FROM\_EMAIL   & Sender address for password resets \\
MAILGUN\_API\_KEY       & Mailgun inbound webhook key \\
MAILGUN\_DOMAIN         & Mailgun sandbox domain \\
GROQ\_API\_KEY          & Groq AI API key (gsk\_\ldots) \\
GEMINI\_API\_KEY        & Optional fallback LLM \\
CLOUDINARY\_CLOUD\_NAME & Cloudinary account name \\
CLOUDINARY\_API\_KEY    & Cloudinary upload key \\
CLOUDINARY\_API\_SECRET & Cloudinary upload secret \\
PORT                    & HTTP server port (default 8082) \\
\bottomrule
\end{tabularx}
\caption{Backend Environment Variables}
\end{table}

\subsection{Mobile (\texttt{pulseboard\_mobile/.env})}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\ttfamily\small}l X}
\rowcolor{tableheadbg}
\color{white}\normalfont Variable & \color{white}Purpose \\
\toprule
EXPO\_PUBLIC\_API\_URL            & Backend base URL — must be local network IP, not localhost \\
EXPO\_PUBLIC\_GOOGLE\_WEB\_CLIENT\_ID & Google web client ID for OAuth flow \\
\bottomrule
\end{tabularx}
\caption{Mobile Environment Variables}
\end{table}

\begin{warnbox}{Local Development Note}
The \code{EXPO\_PUBLIC\_API\_URL} must use your machine's local network IP (not \texttt{localhost}) since the Expo app runs on a physical device. Run \texttt{ipconfig getifaddr en0} on macOS to get the current IP. This IP changes on every network reconnect.
\end{warnbox}

\section{Expo App Configuration}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\ttfamily\small}l X}
\rowcolor{tableheadbg}
\color{white}\normalfont Key & \color{white}Value / Notes \\
\toprule
name / slug        & pulseboard\_mobile \\
scheme             & com.anonymous.pulseboard-mobile \\
version            & 1.0.0 \\
orientation        & portrait \\
newArchEnabled     & false (React Native New Arch disabled) \\
plugins            & expo-router, expo-asset, expo-notifications, datetimepicker \\
ios bundleId       & com.anonymous.pulseboard-mobile \\
android package    & com.anonymous.pulseboard\_mobile \\
android extra      & useNextNotificationsApi: true \\
\bottomrule
\end{tabularx}
\caption{pulseboard\_mobile/app.json — Key Configuration Fields}
\end{table}

\section{Running the Project}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{l l X}
\rowcolor{tableheadbg}
\color{white}Step & \color{white}Location & \color{white}Command / Action \\
\toprule
1 & backend  & \texttt{cd pulseboard\_backend \&\& npm install} \\
2 & backend  & Copy \texttt{.env.example} → \texttt{.env}, fill all variables \\
3 & backend  & \texttt{npm run dev} — starts on port 8082 \\
4 & mobile   & \texttt{cd pulseboard\_mobile \&\& npm install} \\
5 & mobile   & Set \texttt{EXPO\_PUBLIC\_API\_URL} to local machine IP \\
6 & mobile   & \texttt{npx expo start} — scan QR with Expo Go \\
7 & mobile   & \texttt{npx expo start --clear} — use after git pulls \\
\bottomrule
\end{tabularx}
\caption{Development Setup Steps}
\end{table}

% ─────────────────────────────────────────────────────────────
\chapter{Known Issues \& Future Work}
% ─────────────────────────────────────────────────────────────

\section{Current Limitations}

\begin{table}[H]
\centering
\rowcolors{2}{rowodd}{roweven}
\begin{tabularx}{\textwidth}{>{\bfseries}l X X}
\rowcolor{tableheadbg}
\color{white}Issue & \color{white}Description & \color{white}Proposed Fix \\
\toprule
Race Condition     & Two users with same email can both get cache miss simultaneously, causing 2 Groq calls & Redis \texttt{SETNX} distributed lock on \texttt{emailMessageId} \\
5-min Latency      & Events can take up to 5 minutes to appear after email arrives & Gmail Push Notifications via Google Cloud Pub/Sub \\
Token Security     & \texttt{googleAccessToken} stored in plaintext in MongoDB & AES-256 encryption at rest \\
Token Revocation   & User revokes Google access — watcher silently fails & Detect \texttt{invalid\_grant}, clear tokens, notify user to re-auth \\
AI Hallucinations  & Llama may extract wrong dates or miscategorize emails & Post-processing validation + user ``mark as not event'' feedback \\
Gmail Quota        & 1000+ users × 50 emails × every 5min = possible quota exhaustion & Per-user rate limiting + exponential backoff \\
Cold Start Delay   & New users wait up to 5 min for first inbox scan & Trigger \texttt{checkUserEmails()} immediately after Google OAuth \\
IITJ Email Lock    & Registration restricted to \texttt{@iitj.ac.in} — may need updating & Configurable email domain allowlist \\
\bottomrule
\end{tabularx}
\caption{Known Issues and Proposed Fixes}
\end{table}

\section{Feature Roadmap}

\begin{itemize}[leftmargin=*]
  \item \textbf{Real-time Gmail Webhooks} — Replace polling with Gmail Pub/Sub push notifications for sub-30-second event detection
  \item \textbf{Club Chat} — In-app messaging channel per club
  \item \textbf{Event RSVP} — Students confirm attendance; clubs see headcount
  \item \textbf{AI Event Suggestions} — Personalized recommendations based on following patterns
  \item \textbf{Offline Support} — Cache last-fetched events in AsyncStorage
  \item \textbf{Dark/Light Theme Toggle} — Currently locked to dark mode
  \item \textbf{EAS Build} — Expo Application Services for OTA updates and production builds
  \item \textbf{Web Dashboard} — Club admin portal as a web app
  \item \textbf{Analytics} — Event engagement, follow trends, inbox accuracy metrics
\end{itemize}

% ─────────────────────────────────────────────────────────────
\chapter{Dependency Reference}
% ─────────────────────────────────────────────────────────────

\section{Backend Dependencies}

\begin{longtable}{>{\ttfamily}l l p{7cm}}
\rowcolor{tableheadbg}
\color{white}\normalfont Package & \color{white}Version & \color{white}Purpose \\
\endfirsthead
\rowcolor{tableheadbg}
\color{white}\normalfont Package & \color{white}Version & \color{white}Purpose \\
\endhead
\toprule
\rowcolor{rowodd}  express              & 4.21.2   & HTTP web framework \\
\rowcolor{roweven} mongoose             & 9.1.5    & MongoDB ODM + schema validation \\
\rowcolor{rowodd}  cors                 & 2.8.6    & Cross-Origin Resource Sharing \\
\rowcolor{roweven} morgan               & 1.10.1   & HTTP request logging \\
\rowcolor{rowodd}  jsonwebtoken         & 9.0.3    & JWT generation and verification \\
\rowcolor{roweven} bcryptjs             & 3.0.3    & Password hashing (bcrypt) \\
\rowcolor{rowodd}  google-auth-library  & 10.5.0   & Google OAuth 2.0 token handling \\
\rowcolor{roweven} axios                & 1.13.6   & HTTP client (Groq API, Gmail API) \\
\rowcolor{rowodd}  nodemailer           & 8.0.3    & SMTP email (OTP delivery) \\
\rowcolor{roweven} @sendgrid/mail       & 8.1.6    & SendGrid API (password resets) \\
\rowcolor{rowodd}  cloudinary           & 1.41.3   & Image CDN storage \\
\rowcolor{roweven} multer               & 2.1.1    & Multipart file upload middleware \\
\rowcolor{rowodd}  node-cron            & 4.2.1    & Cron-style scheduled tasks \\
\rowcolor{roweven} chrono-node          & 2.9.0    & NLP date parsing (fallback) \\
\rowcolor{rowodd}  helmet               & 8.1.0    & HTTP security headers \\
\rowcolor{roweven} compression          & 1.8.1    & Gzip response compression \\
\rowcolor{rowodd}  dotenv               & 17.2.3   & Environment variable loading \\
\rowcolor{roweven} typescript           & 5.9.3    & TypeScript compiler \\
\bottomrule
\end{longtable}

\section{Mobile Dependencies}

\begin{longtable}{>{\ttfamily}l l p{7cm}}
\rowcolor{tableheadbg}
\color{white}\normalfont Package & \color{white}Version & \color{white}Purpose \\
\endfirsthead
\rowcolor{tableheadbg}
\color{white}\normalfont Package & \color{white}Version & \color{white}Purpose \\
\endhead
\toprule
\rowcolor{rowodd}  react-native              & 0.81.5   & Core mobile framework \\
\rowcolor{roweven} expo                      & 54.0.33  & Managed React Native toolchain \\
\rowcolor{rowodd}  expo-router               & 6.0.23   & File-based navigation \\
\rowcolor{roweven} nativewind                & 4.2.1    & Tailwind CSS for React Native \\
\rowcolor{rowodd}  moti                      & 0.30.0   & Animation primitives \\
\rowcolor{roweven} react-native-reanimated   & 4.1.1    & Native animation driver \\
\rowcolor{rowodd}  lucide-react-native       & 0.563.0  & Icon library \\
\rowcolor{roweven} expo-image                & 3.0.11   & Optimized image component \\
\rowcolor{rowodd}  expo-linear-gradient      & 15.0.8   & Linear gradient component \\
\rowcolor{roweven} axios                     & 1.13.2   & HTTP API client \\
\rowcolor{rowodd}  @react-native-async-storage/async-storage & 2.2.0 & Local key-value store \\
\rowcolor{roweven} expo-notifications        & 0.32.16  & Push notification handling \\
\rowcolor{rowodd}  expo-auth-session         & 7.0.10   & OAuth session management \\
\rowcolor{roweven} expo-web-browser          & 15.0.10  & In-app browser for OAuth \\
\rowcolor{rowodd}  react-native-responsive-screen & 1.4.2 & \texttt{wp()} / \texttt{hp()} responsive units \\
\rowcolor{roweven} react-native-user-avatar  & 1.0.8    & Avatar component \\
\rowcolor{rowodd}  react-native-safe-area-context & 5.6.0 & Safe area insets \\
\rowcolor{roweven} @react-native-community/datetimepicker & 8.4.4 & Native date/time picker \\
\bottomrule
\end{longtable}

% ─────────────────────────────────────────────────────────────
%  End of Document
% ─────────────────────────────────────────────────────────────
\newpage
\begin{center}
\vspace*{8cm}
{\Huge\bfseries\color{darkbg} END OF DOCUMENTATION}\\[1cm]
\textcolor{accent}{\rule{8cm}{2pt}}\\[0.6cm]
{\large\color{gray}\texttt{PulseBoard v1.0.0 · IIT Jodhpur · 2026}}\\[0.3cm]
{\small\color{gray}\texttt{SYS.READY · ALL IN. ALL OUT.}}
\end{center}

\end{document}
